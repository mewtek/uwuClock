package;

class Settings
{
    /* 
        TODO:
        This needs to be done in a JSON format because we don't have the luxury of doing this
        with flixel's save.data stuff. It might be able to work better if this is runnning on
        HTML5, but other than that I'm not sure what else to do other than make a JSON System for it.
    */
}